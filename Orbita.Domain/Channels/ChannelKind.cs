namespace Orbita.Domain.Channels;

/// <summary>Mirrors orbita-schema.dbml's channel_kind enum. TikTok is phase 1 and has no adapter yet.</summary>
public enum ChannelKind
{
    WhatsApp,
    Instagram,
    TikTok,
}
